#include "AIManager.hpp"

AIManager::AIManager() {
	// this->state.Load(LUA_FILE);
	// if (state["test"] == 5) {
	//	std::cout << "Good to go !" << std::endl;
	// } else {
	//	std::cerr << LUA_FILE << ": check didn't pass." << std::endl;
	// }
}
